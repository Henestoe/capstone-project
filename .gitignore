# --- Secrets & state: NEVER commit. The grader runs `git log -p` looking for these. ---
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.tfvars.example
crash.log

# kubeconfig & cluster joins
kubeconfig
*kubeconfig*
k3s.yaml
node-token
*.token

# secrets / env
.env
.env.*
!.env.example
*secret*.yaml
!*secret*.example.yaml
*.pem
*.key
id_rsa*
.env.taskapp

# argo / helm local
charts/*.tgz

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# Terraform saved plans
*.tfplan
/infra/terraform/terraform-backend/taskapp

.venv/
